namespace UI
{
    public partial class PasscodePasswordWindow : Form
    {
        public PasscodePasswordWindow()
        {
            InitializeComponent();
            this.StartPosition = FormStartPosition.CenterScreen; // Đặt form ở giữa màn hình (từ yêu cầu trước)
            this.AutoScaleMode = AutoScaleMode.Dpi; // Tự động điều chỉnh theo DPI màn hình
        }

        private void PasscodePasswordWindow_Load(object sender, EventArgs e)
        {

        }
    }
}
